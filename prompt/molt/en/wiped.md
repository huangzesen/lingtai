# Context Wiped — Recovery Mode

[system] Context has been wiped by the system.

You ignored all molt warnings. Your conversation history is gone.

**Recovery steps:**

1. **Check your persistent knowledge:**
   - `email(check)` — check your inbox for recent messages
   - `email(search, query=...)` — search for specific information
   - `library(filter, pattern=...)` — retrieve stored insights

2. **Load your identity and memory:**
   - `psyche(lingtai, load)` — restore your character/identity
   - `psyche(memory, load)` — restore your working notes

3. **Start fresh:**
   Your previous context is gone, but your identity and stored knowledge remain.
   Check mail and library for context about what you were working on.

**Going forward:** When you receive molt warnings, act on them immediately. Save important findings to library, update your identity and memory, and write a proper briefing before molt.
